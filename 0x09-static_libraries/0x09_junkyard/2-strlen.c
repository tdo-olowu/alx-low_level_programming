#include "main.h"

/**
 * _strlen - measures a string by its number of characters.
 * @s: the string in question, stored in an array.
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int len;

	for (len = 0 ; s[len] != '\0' ; ++len)
	{
	}
	return (len);
}
