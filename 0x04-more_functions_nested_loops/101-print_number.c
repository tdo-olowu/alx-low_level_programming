#include "main.h"

/**
 * print_number - prints a number by scanning it character to char
 *
 * My approach is to compute the integer log of the number,
 * printing the digits in the process.
 *
 * @n: the number in question
 * Return: no return value
 */
void print_number(int n)
{
	int i, log, d;

	i = n;
	_putchar(i);
}
