#include "search_algos.h"

/**
 * exponential_search - searches for an element in an array
 * using exponential search.
 *
 * @array: pointer to the head of the array to search
 * @size: the number of items in the array
 * @value: the value to be searched for, i.e. the target
 * Return: index of the value, or -1 if it's not present.
 */
int exponential_search(int *array, size_t size, int value)
{
	(void)array;
	(void)size;
	(void)value;

	return (-1);
}
