#include "search_algos.h"

/**
 * linear_skip - linear search with a skip list
 * @list: the skip list to skip
 * @value: the value to be searched for.
 * Return: pointer to the taret.
 */
skiplist_t *linear_skip(skiplist_t *list, int value)
{
	return (NULL);
}
